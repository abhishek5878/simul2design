# Handoff — 2026-04-26

Supersedes [handoff-2026-04-23-late.md](handoff-2026-04-23-late.md). Two days passed; the project advanced significantly through the matrix v2 cascade on 2026-04-24, and process debt was closed today.

## TL;DR

Univest V5 v2 is the corrected, decisive deliverable. Predicted 50.6% (mechanism range 44–56%, Wilson envelope 22–74%) vs V4's 44%. Untested-stack down to 1 element. Adversary blockers down to 0 (2 operational preconditions remain — Univest-side commitments, not design problems). The full pipeline + observability + immutable evaluator + sim-flow status are all honest about v2 state.

## State of the tree

- Branch: `main`. Tracks `origin/main`. Clean (assuming this session's commit lands).
- Last 6 commits:
  - (this session) — Catch up session persistence + sim-flow v2 schema awareness
  - `a7f55c1` Matrix v2: screenshot re-extraction + corrected V5 cascade
  - `c8701ba` Sharpen V5 spec to decisive single-design with audience reasoning
  - `530eef2` Adopt simulator provenance + cite related-work in estimator and spec
  - `f071abc` Session-end ritual: progress log + new handoff
  - `cb6d1f0` sim-flow: add record-actuals verb and immutable-evaluator loop

## Active plans

**None.** `tasks/todo.md` active-plan pointer is "None." All `tasks/*-plan.md` files have phases marked `complete`.

## The one command you should know

```bash
scripts/sim-flow.py status univest
```

Reads the whole pipeline in <2 seconds. Now v2-aware (as of this session). If you don't know where to start — start there.

## What's in V5 right now

- **Visual:** [`data/univest/design/v5a-green.png`](../data/univest/design/v5a-green.png) — dark theme, dual CTA preserved with copy fix (`See 3 real trades, free` outline + `Activate for ₹1` sticky), wins/losses transparency (914W·62L), explicit "Refund in 60s to source" SLA, no countdown timer, real ZOMATO closed trade card, "Browse all" cherry-picking-defense link.
- **Buildable spec:** [`data/univest/v5-spec.md`](../data/univest/v5-spec.md) — 5 components, 2 operational preconditions, 12 instrumentation events with kill thresholds.
- **Evidence chain:** matrix → weighted_scores → synthesized_variant → adversary_review → conversion_estimates → spec → visual. Every artifact has a v2 marker; v1 artifacts referenced in audit trails.
- **Source ground truth:** [`data/univest/source-screenshots/`](../data/univest/source-screenshots/) — 5 immutable PNGs of the actual variants. Plus [`source-v2.md`](../data/univest/source-v2.md) — screenshot-validated extraction. Original `source.md` preserved for audit.

## Per-segment "audience likes this" (corrected)

| Segment | Weight | V4 → V5 |
|---|---|---|
| Skeptical Investor | 24% | 25% → 35% (5 trust barriers removed in one pass) |
| Curious Beginner | 30% | 33% → 41% (V1's named-stock anchor restored) |
| Bargain Hunter | 26% | 69% → 69% (V4's strongest elements preserved at full strength) |
| Trust Seeker | 20% | 50% → 60% (V1 trust stack restored on V4's premium dark theme) |
| **Weighted overall** | — | **44% → 50.6%, +6.6pt median** |

## Two operational preconditions (Univest-side)

These are the ship gates. Not design problems — operational commitments Univest must make.

1. **"See 3 real trades, free" outline-CTA flow MUST actually deliver 3 closed trades pre-payment** (no payment screen, no KYC gate). If broken, instrumentation event `v5_payment_required_before_trade` fires → stop-ship immediately.
2. **"Refund in 60s to source. No questions." copy MUST match operational reality per payment method.** Submit a refund-SLA matrix (observed p90 elapsed-time per payment method, last 30 days) before ship. If UPI is 30s but card is 5 days, copy must change.

## Next work (user's choice)

Three paths possible; none active:

1. **Post-ship Univest** — when V5 ships and actuals come back:
   ```bash
   scripts/sim-flow.py record-actuals univest <actuals.json>
   ```
   Schema in the `record-actuals` docstring at `scripts/sim-flow.py`. Freezes predicted (immutable), records actual, computes delta, surfaces calibration signal in `status`.

2. **Second client engagement** — the genericity test. Run the full pipeline against a non-Univest client without any Univest-specific code edits. Expected pain points: new taxonomy overlay, possibly new dimensions to lift into base. Three Open improvements in `improvements.md` are gated on this trigger.

3. **Weekly self-edit ritual** — first run scheduled 2026-04-30 per `.claude/self-edit/weekly-ritual.md`. 4 days away.

## Open improvements (top 5 worth knowing)

Full list in `tasks/improvements.md` Open section (12 entries). Highest-leverage:

1. **Audit Univest VoC grounding sources** (should-fix, PersonaCite-derived) — would tighten V5 confidence intervals retroactively if Univest hands over real reviews/churn-survey data.
2. **Inferred values are easy to miss** (should-fix) — synthesis skill should refuse load-bearing decisions on values flagged `extraction_confidence: inferred`.
3. **Stock selection is out-of-matrix context** (should-fix) — V1's named-wins effect depends on user pre-existing relationship with the stocks shown. V5's carousel will name different stocks; the anchor mechanism may not transfer.
4. **Non-linearity discount factor (0.7) is assumed, not calibrated** (should-fix) — needs cross-client actuals to fit a real function.
5. **Persona diversity audit skill** (nice-to-have, Persona-Generators-derived) — second-client trigger.

## Known unknowns

- Whether V1's named-wins anchor mechanism (TMPV/ZOMATO/RELIANCE) transfers when V5's carousel names different stocks. Curious Beginner's +7pt depends on stock-selection resonance. Filed as Open improvement.
- Whether removing the countdown timer costs Bargain Hunter conversion (timer may have helped that segment in V1; V4 demonstrated structural urgency works without timer, but partially confounded). Adversary obj-006 in v2 review, watch in instrumentation.
- Simulator LLM is undisclosed by Apriori — `simulator_provenance: "unknown"` in conversion_estimates.json. Per Lost in Simulation 2026, this downgrades overall confidence one tier.

## If you're picking this up — first 30 minutes

1. `scripts/sim-flow.py list` (1 sec) → see clients.
2. `scripts/sim-flow.py status univest` (2 sec) → see state. Should be clean v2 output.
3. Read `tasks/progress.md` last 4 entries (10 min) — the matrix v2 cascade and this catch-up session.
4. Read `tasks/lessons.md` + `tasks/improvements.md` Open section (5 min).
5. Open `data/univest/design/v5a-green.png` side-by-side with `data/univest/v5-spec.md` (5 min).
6. Run the session-start ritual from `.claude/rules/session-persistence.md`. Pick one thing.

If Univest actuals arrived:

```bash
scripts/sim-flow.py record-actuals univest <path-to-actuals.json>
scripts/sim-flow.py status univest    # post-ship evaluator block now populated
```

Schema for the actuals file is in the `record-actuals` docstring at `scripts/sim-flow.py`.

## Verify output (session-end)

To be filled at commit time after this session's `verify` pass.

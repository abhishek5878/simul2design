# Handoff — 2026-04-23

## TL;DR

Univest V5 synthesis is **design-complete** and pushed to [github.com/abhishek5878/simul2design](https://github.com/abhishek5878/simul2design). End-to-end pipeline shipped (parse → weigh → synthesize → adversary → estimate → spec → visual mockup). The engineer-facing deliverable is [`data/univest/v5-spec.md`](../data/univest/v5-spec.md); the visual is [`data/univest/design/v5a-green.png`](../data/univest/design/v5a-green.png) (primary) + [`v5b-muted-premium.png`](../data/univest/design/v5b-muted-premium.png) (exploratory). **V5 ship is blocked on Univest-internal legal/ops/product sign-off of 3 Operational Preconditions** — not on the synthesis pipeline.

## State of the tree

- Branch: `main`. Tracks `origin/main`. **Clean** (no uncommitted, no unpushed).
- 5 commits on origin:
  - `73a12e2` Update README and progress log (end-of-session state)
  - `401c44a` Univest V5: end-to-end synthesis + buildable design + visual mockups
  - `aba9e54` Build Multiverse Synthesis Engine pipeline: 5 skills + adversary agent
  - `3c0ee7c` Scaffold the four-layer Claude Code setup per SETUP.md
  - `b4b115c` Initial commit: empty scaffold
- No worktrees, no stashes, no risky state.

## Active plans

**None.** `tasks/todo.md` active-plan pointer is "None." Every `tasks/*-plan.md` has all phases marked `complete` with Review + Errors Encountered sections filled.

## Next work (user's choice)

Three paths are possible; none is active:

1. **Post-ship Univest** — when Univest ships V5 and actuals come back, record predicted-vs-actual delta. Most improvements.md Open entries resolve here (non-linearity discount calibration, coupled-mechanism validation, stock-selection impact).
2. **Second client** — the genericity test. Run the full pipeline end-to-end on a different domain. Expected pain points: new overlay taxonomy, new contradictions, potentially new dimensions to lift into `element-taxonomy-base.md`.
3. **Research / observation layer activation** — currently wired but unused. See improvements.md.

## Deferred (improvements.md Open — top 4 by severity)

1. **V4.refund_or_guarantee_copy source inconsistency** — V4 Skeptical quote mentions refund, but source tags V4 as `absent`. Blocker if V5 synthesis mis-prioritizes refund copy. Fix: visual verification of V4's live UI.
2. **Non-linearity discount not calibrated** — synthesize uses 0.7 by feel. Post-ship actuals from Univest or a second client calibrate it.
3. **Stock-selection out-of-matrix context** — `PastWinsCarousel` mechanism depends on which stocks resonate with the real user cohort. Needs real user analytics.
4. **Research/observation hooks unused** — autoresearch cron not scheduled; correction log not yet audited. Weekly self-edit ritual scheduled for 2026-04-30.

## Known unknowns (surfaced this session)

- Does Univest have operational capacity to honor the refund SLAs published in `RefundSlaLine` copy? (Adversary Precondition 2.)
- Does the "See 1 real trade, free" flow actually deliver a free trade view before the ₹1 step? (Adversary Precondition 1.)
- What's the actual win rate of Univest's recent closed trades? The spec's `ClosedTradeCard` requires at least 1 winning trade within 24h to render; if win rate is low the card hides frequently.
- Segment weights (24/30/26/20) come from the simulation. Real Univest user base composition may differ — would change V5 recommendation between green (balanced) and muted_premium (exploratory).

## If you're picking this up — first 30 minutes

1. **Read `README.md`** (2 min). Orients you to the project shape.
2. **Read `tasks/progress.md` last 3 entries** (5 min). Captures the week's work.
3. **Read `tasks/lessons.md` + `tasks/improvements.md` Open section** (5 min). The operating disciplines + known weaknesses.
4. **Open [`data/univest/design/v5a-green.png`](../data/univest/design/v5a-green.png)** and [`data/univest/v5-spec.md`](../data/univest/v5-spec.md) side-by-side (5 min). The engineer-facing deliverable.
5. **Before touching anything, run the session-start ritual** from `.claude/rules/session-persistence.md`. Skim lessons, check in_progress phases (none), pick one thing.

If you're continuing for a second client:
- Start `parse-simulation` with the client slug.
- Do NOT modify `element-taxonomy-base.md`. Create `element-taxonomy-<slug>.md` instead.
- Land data under `data/<slug>/`.
- Run the full pipeline. Each skill is client-agnostic — reads the slug from the matrix.

If you're continuing for Univest post-ship:
- When actuals arrive, compare to `data/univest/conversion_estimates.json` per-segment predictions + kill-conditions.
- The evaluator is immutable. Do NOT rewrite `conversion_estimates.json` — create a new file `data/univest/post_ship_actuals.json` and a `data/univest/predicted_vs_actual.md` that references both.

## Verify output (2026-04-23)

All content artifacts validate. No test suite yet (no code). See session-end ritual output in `tasks/progress.md`.
